# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: kaizen-toolbox
metadata:
  template: kaizen-toolbox@0.1.0

services:
  web:
    project: ./dotnet
    language: csharp
    host: appservice

